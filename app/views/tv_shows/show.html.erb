<h1><%= t ".title" %></h1>
<tr>
  <td><%= @tv_show.name %></td>
  <td><%= @tv_show.info %></td>
  <td><%= image_tag @tv_show.poster.url if @tv_show.poster? %></td>
</tr>
<tr>
  <td><%= link_to t(".edit"), edit_tv_show_path(@tv_show) %></td>
  <td><%= link_to t(".destroy"), @tv_show, method: :delete, data: {confirm: t(".sure")} %></td>
</tr>
